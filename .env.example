# Never put your API key inside frontend code.
# Create a fresh key after revoking any key that was shared in chat.
OPENAI_API_KEY=sk-proj-your-new-key-here
OPENAI_MODEL=gpt-5.5

# Set true to test the UI without calling OpenAI.
USE_MOCK=false

# Optional port
PORT=8787
